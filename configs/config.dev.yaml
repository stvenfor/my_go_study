# config.dev.yaml 开发环境覆盖项（本机 make run 默认连 localhost）。
# Docker 部署时由 docker-compose.yml 注入 DATABASE_HOST=postgres 等环境变量覆盖。

server:
  mode: debug

log:
  level: debug

database:
  host: localhost

redis:
  addr: localhost:6379

# Supabase 常量见 configs/supabase.env（入库）；service_role 见 .env.local
