# config.dev.yaml 开发环境覆盖项（本机 make run 默认连 localhost）。
# Docker 部署时由 docker-compose.yml 注入 DATABASE_HOST=postgres 等环境变量覆盖。

server:
  mode: debug

log:
  level: debug

database:
  host: localhost

redis:
  addr: localhost:6379

supabase:
  url: https://uqznnzkugvhsrlcudrbj.supabase.co
  anon_key: sb_publishable_b6_-AnEAi19hTurNNu7ojA_sL4oHgf5
  # 可选：配置 SUPABASE_SERVICE_ROLE_KEY 环境变量以区分「未注册」与「密码错误」
  service_role_key: ""
