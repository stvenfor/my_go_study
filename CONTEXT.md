# my_go_study — 本机局域网后端与本地认证

本仓库在「家庭/信任 Wi‑Fi 上，用开发者 Mac 充当 Flutter 可连的 BFF」以及「本地 Auth/Postgres 可迁回 Supabase」上下文中的用语。

## Language

**局域网后端主机 (LAN Backend Host)**：
在同 Wi‑Fi 下向真机提供 Go BFF 的那台 Mac（按需启停，不是公网服务器）。
_Avoid_: 生产服务器、云主机、公网部署

**LAN 配置档 (LAN Profile)**：
局域网真机联调姿态；现语义为本地全栈（`auth.provider=local`）+ 局域网暴露。
_Avoid_: LAN + Supabase Cloud 混用、直接把 prod 当作家用联调

**本地认证后端 (Local Auth Provider)**：
`auth.provider=local`：本机 Postgres `auth_users` 签发 UUID JWT，业务表在本机；`supabase` 为可切回的 Cloud 路径。
_Avoid_: 把自托管 GoTrue 说成「只要 Postgres」

**固定局域网地址 (Pinned LAN Address)**：
为该 Mac 稳定下来的 IPv4（DHCP 预留或静态），供 HTTP 与 Realtime 主机名共用。
_Avoid_: 每次手抄漂移 IP、把家庭 IP 提交进 Git

**本机密钥与主机文件 (Local Host Env)**：
仅存在于开发者机器、不入库的环境文件；存放真实 LAN IP、测试白名单等。
_Avoid_: 把真实 IP/白名单写进可提交的 yaml

**联调白名单账号 (Lab Whitelist Account)**：
允许在单设备策略下多端同时在线的指定测试账号；普通账号仍单设备。
_Avoid_: 局域网模式下全局关闭单设备限制

**明文局域网传输 (Cleartext LAN Transport)**：
在信任局域网内使用 `http`/`ws`；HTTPS/`wss` 为后续可选加固，不是当前前提。
_Avoid_: 一上来就要求内网证书/反代才能联调

## 门店与身份

**门店 (Store)**：
多名用户可以同属的一家组织。一名用户也可以同属多家门店。
_Avoid_: 个人统计名片、把 `wys_user_store_stats` 的一行当成店

**门店职务 (Store Position)**：
某人在某家门店的岗位标签（销售顾问、销售经理、总经理）。不是访问权限。
_Avoid_: 角色、role、权限角色

**权限角色 (Access Role)**：
决定能做什么的角色。不是门店职务。
_Avoid_: 职务、岗位、统计卡上的 role

**平台角色 (Platform Role)**：
不挂任何门店的权限角色。
_Avoid_: 店内角色、超级管理员职务

**店内角色 (Store Role)**：
必须挂在某一家门店上的权限角色。离开那家店就不生效。
_Avoid_: 平台角色、门店职务

**角色分配 (Role Assignment)**：
把一个权限角色授给一个用户。平台分配不属于任何门店；店内分配只在指定的那一家门店成立。同一套角色定义各店共用。
_Avoid_: 把店写在角色定义上、每店复制一套角色

**当前店 (Current Store)**：
这个用户此刻正在操作的那一家门店。
_Avoid_: 他所属的全部门店、统计卡上的 store_id

**有效权限 (Effective Permissions)**：
平台角色能做的事始终算数，不依赖当前店，也不要求是成员。店内角色能做的事，只有在当前店、且他是该店成员时才算数。账号能不能登录不由角色决定。
_Avoid_: 只看职务、平台管理员必须先成为成员才能管店

**门店成员 (Store Member)**：
一个用户属于某一家门店的身份，上面有且只有一个门店职务。不是该店成员，就不能持有这家店的店内角色。
_Avoid_: 统计卡、权限角色、一人兼多个职务

**门店统计 (Store Stats)**：
某个用户在某家门店的展示数字，每人每店至多一行。不是成员名单，也不是职务来源。
_Avoid_: 一店一行、从统计卡读职务、把统计卡上的人当成成员

**权限 (Permission)**：
可以授给权限角色的一项能力。作用范围包含在这项能力的含义里。多角色只做并集，没有「拒绝」。
_Avoid_: 菜单、职务、账号状态

**内置角色 (Built-in Role)**：
随迁移发布的权限角色。运行时不能新建角色，也不能改它上面的权限。
_Avoid_: 店内自建角色、运行时角色目录

## 新车成交

**新车成交 (New Car Deal)**：
销售在当前店为自己提交的新车成交发票业务；首页入口文案为「新车成交」。列表与统计只看本人在当前店上传的发票。
_Avoid_: 新车关注、店务审核单、商城订单、门店全员共用列表

**成交发票 (Deal Invoice)**：
一条新车成交的发票审核单：绑定购车客户、可选占位图 URL、审核态与提交时间；可驳回后同单重提。
_Avoid_: 真实发票 OCR、对象存储上传、店务审核单、商城订单

**成交发票审核态 (Deal Invoice Status)**：
四态：待审核、已通过待评价、已通过已评价、未通过。本阶段无审核写接口；状态靠种子或手工改库。重提把同一条改回待审核并清空驳回原因。
_Avoid_: 服务端「上传中」、客户端本地 uploading 当库态、本切片做通过/驳回 API

**购车客户 (Deal Customer)**：
门店客户在成交场景下的选用身份；数据落在 `wys_store_customer`（含必填手机号）。成交发票用 `customer_id` 关联，不是另建客户表。
_Avoid_: 仅逾期待跟进子集、门店统计 total_customers、登录用户 Profile

**成交页摘要 (Deal Invoice Summary)**：
成交列表顶栏一次返回的读模型：当前用户在当前店的展示名、头像、门店职务文案、店名，以及本人发票四格统计（已上传/待审核/已通过/未通过）。
_Avoid_: 门店统计卡数字、再打 Mine 多个接口拼顶栏

## 小视频

**小视频 (Short Video)**：
用户发布的短视频内容单元；有标题、封面、播放地址、时长与审核态；可可选关联一个话题。与社区「动态」是不同实体，互不写入对方列表。
_Avoid_: 社区视频帖、media_type=video 的动态、Reel

**小视频审核态 (Short Video Status)**：
小视频对外可见性的状态：审核中、已通过（正常）。发布后先为审核中；经过约定时延后自动变为已通过。公开发现列表只含已通过；作者自己的列表可含审核中。
_Avoid_: 上传中 uploading 当服务端态（客户端本地态）、软删除当审核态、人工审核台（本期无）

**小视频发现流 (Short Video Discovery Feed)**：
全站已通过小视频的列表视图；与个人列表共用同一列表查询，用 scope 区分。
_Avoid_: 社区动态流 Tab、独立发现专用资源路径当唯一真相

**小视频个人列表 (Short Video Profile List)**：
某一用户名下的小视频网格视图；作者本人可见审核中，他人只见已通过。与发现流共用同一列表查询。
_Avoid_: 发现流、门店统计、与发现拆成两套互不相干的契约

**小视频点赞 (Short Video Like)**：
用户对一条小视频的有向点赞关系；可取消。计入该视频与作者维度的点赞统计。
_Avoid_: 社区动态点赞、播放量

**小视频播放量 (Short Video View Count)**：
小视频被播放的累计次数；每次播放上报都加一，不做每用户去重。
_Avoid_: 独立播放流水当列表真相、与点赞混计

**小视频个人主页 (Short Video Profile)**：
某一用户在小视频场景下的展示名片：展示名、头像与视频数/播放量/点赞数统计；与门店职务徽章无强制绑定。
_Avoid_: 门店统计卡、社区作者卡片当唯一来源

## 社区动态

**动态 (Post)**：
社区里一条用户发布的内容单元；可带正文、可选媒体、可选关联话题。
_Avoid_: 帖子 feed item、朋友圈、说说

**媒体类型 (Media Type)**：
一条动态上媒体的形态：无媒体、仅图片、仅视频；图与视频互斥。
_Avoid_: 附件类型、混排 media、图+视频同帖

**话题 (Topic)**：
可被动态关联的社区话题实体；展示名带 `#`，可检索、可统计热度。
_Avoid_: 正文里的任意 hashtag 字符串、Realtime topic、频道

**关联话题 (Topic Association)**：
一条动态或一条小视频与至多一个话题实体之间的绑定；未关联时可为空。小视频与动态共用同一话题目录。
_Avoid_: 多话题挂载、把正文 `#` 解析结果当成关联本身、小视频私有话题表

**问大家 (Ask Everyone)**：
一种特殊发帖模式：动态带 `is_ask_everyone`；本阶段无真实持仓域，发帖后向种子用户列表投递 `sys.notify` 邀请来回答（评论即回答）。
_Avoid_: 真实持仓匹配、门店职务 position、普通话题

**社区公约弹窗 (Convention Dialog)**：
发布页上的知悉弹窗；纯客户端控制，每天最多展示一次，展示记录只存在客户端。
_Avoid_: 服务端 ack、发帖前置校验、法律签署

**动态流 Tab (Feed Tab)**：
社区列表顶栏三个视图：最新（按发布时间）、热门（按 `heat`）、关注（仅已关注作者）。对应查询参数 `tab=latest|hot|following`。
_Avoid_: 客户端本地假排序、三个独立接口硬拆

**动态热度 (Post Heat)**：
帖子上的排序分：`heat = like_count*2 + comment_count`；点赞/评论写路径维护，热门 Tab 按此降序。
_Avoid_: 话题 heat、时间衰减任务（本切片不做）

**社区关注 (Community Follow)**：
用户 A 关注用户 B 的有向关系，存于 `wys_user_follows`；决定「关注」Tab 可见哪些作者的动态。
_Avoid_: 互关、粉丝数、推荐关注

## 首页待办卡

**首页待办卡 (Home Todo Card)**：
首页上一条可处理的待办/提醒展示单元；有标题、副文案、行动按钮与可选配图。
_Avoid_: 快捷动作 QuickAction、运营 Banner、社区动态、门店统计卡

**待办卡类型 (Todo Card Type)**：
服务端下发的卡种类（如新伙伴待确认）；决定尺寸映射与排序优先级。无对应有效权限或 count 为 0 时不出现。客户端不自创类型。
_Avoid_: 尺寸枚举当初级字段、用标题字符串区分种类、无权限仍占位空卡

**待办卡尺寸 (Todo Card Size)**：
客户端由待办卡类型映射出的占位：小卡=1 格、中卡=2 格、大卡=4 格；一页为 2×2 共 4 格。
_Avoid_: 服务端再下发 size、任意像素宽高

**待办卡页 (Todo Card Page)**：
待办卡区域一次左右滑看到的一屏；由客户端按类型优先级装箱，不是服务端预分页。
_Avoid_: 服务端 pages[]、整页 Feed 分页

**新伙伴待确认 (Partner Pending Confirmation)**：
首页待办卡的一种类型：当前店里等待确认入店的新伙伴；尺寸为大卡。
_Avoid_: 已是门店成员、即时 upsert 成员、运营 Banner

**待跟进客户 (Follow-up Customer)**：
首页待办卡的一种类型：当前店里需要跟进的客户；尺寸为中卡。
_Avoid_: 门店统计上的客户总数、已跟进完毕的客户

**订单待审核 (Order Pending Review)**：
首页待办卡的一种类型：当前店里等待审核的订单；尺寸为小卡。
_Avoid_: 买家商城订单列表、已审核/已关闭订单

**售后预约 (After-sales Appointment)**：
首页待办卡的一种类型：当前店里待处理的售后预约；尺寸为中卡。卡上计数为状态待处理且预约日 ≥ 今天的条数。
_Avoid_: 售后专区入口文案、商城售后非目标、过期未处理撑首页

**入店申请 (Store Join Application)**：
用户申请加入某家门店后、店管确认或拒绝前的待审记录；「新伙伴待确认」的计数来源。
_Avoid_: 管理员直接写入 wys_store_member、已是成员

**店务审核单 (Store Review Order)**：
门店侧待审核的业务单（如新车单），与商城买家订单分离；「订单待审核」的计数来源。
_Avoid_: wys_mall_order、买家「我的订单」

## 签到与积分

**签到 (Check-in)**：
用户在一个自然日内对账号完成的一次每日打卡；同一自然日至多成功一次。
_Avoid_: 打卡作业、课堂 checkin、登录 SignIn

**签到页 (Check-in Page)**：
「我的」入口下的单一业务页：展示签到日历、积分余额、可做任务；路由承接原「签到商城」页面。
_Avoid_: 拆成日历页+商城页、把现有现金商城当成签到页

**每日签到弹窗 (Daily Check-in Dialog)**：
当天首次进入首页、且当日尚未签到时弹出的轻量层；可在弹窗内完成签到，每天至多展示一次。
_Avoid_: 把整页签到页当弹窗、已签到仍打扰

**积分 (Points)**：
账号全局一本可累计、可消耗的虚拟币；签到与任务奖励都入此账。UI 统一称「积分」，不再并列成长值/i车币/金币。
_Avoid_: 按店分账、成长值与 i车币各一本、把积分当成展示别名却另有真币

**商品支付方式 (Product Payment Mode)**：
单个商城 SKU 如何收款：仅人民币、仅积分、或人民币与积分混合。价在 SKU 上：`price_cny` 与 `price_points`（均可为 0）；两价都大于 0 即为混合。混合下单须同时付满人民币标价与积分标价，不可只付一边、也不可积分随意抵扣。
_Avoid_: 签到页独占兑换柜、积分当满减滑动抵扣、价只挂在 SPU

**积分支付 (Points Payment)**：
商城支付渠道之一：扣减账号积分。纯积分订单一笔积分支付单；混合订单为积分支付单 + 人民币渠道支付单，两笔都成功才算已付。积分在支付动作时扣减；失败或取消则全额退回。
_Avoid_: 下单时偷偷扣积分却不记支付单、一张支付单混记两种币、创建订单预扣 hold

**订单支付方式一致 (Order Payment Homogeneity)**：
同一笔商城订单内所有 SKU 必须属于同一商品支付方式（全仅人民币、全仅积分、或全混合）。提交订单时校验，不符则拒绝并提示拆单。
_Avoid_: 一单混装多种支付方式、加购时静默改价

**签到奖励 (Check-in Reward)**：
完成当日签到后记入积分账的奖励。服务端按 `Asia/Shanghai` 自然日判定；固定每日 5 分，连签第 3 日当日共 10 分，第 7 日当日共 20 分。
_Avoid_: 只有文案没有入账、客户端本地加分、跟设备时区算能否签到

**成长任务 (Growth Task)**：
签到页上可完成的行为任务；完成条件挂钩真实业务行为，奖励入同一本积分账。第一期任务为每日一次：每日登录（+5）、发一条社区动态（+10）、成功支付一笔商城订单（+20）。「每日登录」与「每日签到」独立，可同日各领一次。行为达标后须用户点击领取才入账。
_Avoid_: 纯展示 toast 任务、自动静默加分、登录与签到合并、第一期做「回复客户/邀请顾问」

**积分退回 (Points Refund)**：
积分在支付动作时扣减。商城订单在支付失败或买家取消且积分已扣时，将已扣积分全额退回账号。
_Avoid_: 创建订单时预扣 hold、人民币成功回调里再扣分、积分支付概不退回
