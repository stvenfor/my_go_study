# Rule — dual truth sources

验收与完成态 **只认**：

1. 仓库根 [`AGENTS.md`](../../AGENTS.md) 中的硬规则与联调检查项
2. 已落盘的 Slice Brief / 验收清单 / acceptance-record（`plans/` 或 `docs/` 下约定路径）

禁止第三套口头标准。Harness / `.harness` / skill **不另起验收口径**。
