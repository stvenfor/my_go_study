# 小视频独立实体：不复用社区动态，审核态读时惰性过审

小视频与社区「带视频的动态」是不同产品面（网格标题/播放量/时长/审核态 vs 社区卡片正文）。本期新建 `wys_short_videos` 与 `/api/v1/short-videos*`，发布不写入社区 feed。话题复用 `wys_topics`（至多一个 `topic_id`）。无审核台：发布为 `reviewing`，配置时延（默认 30s）后在读路径惰性改为 `normal`；发现流只出已通过。不做真实上传，空 URL 填默认媒体。播放量每次上报 +1（不去重）；点赞独立于社区赞表。
