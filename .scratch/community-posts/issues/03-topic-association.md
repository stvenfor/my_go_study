# 03 — 关联话题（列表 / 搜索 / 发帖绑定）

**What to build:** 用户在发布流程中打开关联话题，能浏览热度列表、搜索、选中至多一个话题（含「问大家」种子项）；发帖后正文带 `#话题名`，列表项带话题信息。本票不做邀请推送。

**Blocked by:** 01 — 纯文字动态：发布 → 社区列表可见

**Status:** done

- [x] `GET /api/v1/community/topics` 与 `.../topics/search` 可用；「问大家」可置顶识别
- [x] 发帖可带 `topic_id`；服务端追加 `#话题名`（不重复）
- [x] 列表读模型含可选 `topic`；一条动态至多一个话题
- [x] Flutter 关联话题页 + 发布绑定；社区/发布展示话题名
- [x] 种子话题含「问大家」及若干热度话题
